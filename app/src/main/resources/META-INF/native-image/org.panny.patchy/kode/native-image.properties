# GraalVM Native Image build arguments embedded into the binary.
# kotlinx.serialization generates serializers at compile time (no reflection),
# and Clikt is largely reflection-free, so the reflect/resource configs are
# currently minimal. Regenerate with the native-image-agent as commands grow
# (design ch. 08).
Args = --no-fallback
